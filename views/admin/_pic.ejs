<% if (p.image_id) { %><img src="/img/<%= p.image_id %>" alt="" loading="lazy">
<% } else if (p.category === 'supply') { %><%- supplyIcon(p.slug) %>
<% } else { %><%- boxSvg(p.dimensions, { label: false }) %><% } %>
